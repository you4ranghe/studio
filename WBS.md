# 스튜디오 — 작업 문서

> **새 세션은 이 파일을 먼저 읽으세요.** 제품 설계와 근거는 [`_plan/기획서.md`](_plan/기획서.md)에 있습니다.

- 최종 업데이트: 2026-08-10
- 무엇: 폼을 채우면 돌잔치·청첩장·행사 퀴즈 페이지를 **HTML 파일 하나로** 뽑아주는 유료 서비스
- 현재 단계: **M1 (파이프라인 증명)** — 서버·로그인·결제 없음
- 출발점: `C:\Users\ibank\quiz` 의 돌잔치 퀴즈. 그 `slides.src.html`이 1번 템플릿이 됐습니다.

---

## 1. 실행

```powershell
cd C:\Users\ibank\studio
npm run dev
```

| 주소 | 화면 |
|---|---|
| http://localhost:5173/app/index.html | 갤러리 |
| http://localhost:5173/app/editor.html?t=dol-quiz | 편집기 |

> `file://`로 열면 안 됩니다. 브라우저가 `fetch()`로 템플릿 파일을 읽어야 해서
> 반드시 개발 서버를 거쳐야 합니다. 의존성은 없고 `dev.js`는 표준 라이브러리만 씁니다.

---

## 2. 구조

```
studio\
├─ WBS.md                    ← 이 문서
├─ _plan\기획서.md            ← 제품 기획 (설계 근거·마일스톤·리스크)
├─ dev.js                    ← 개발 서버 (의존성 없음)
├─ templates\
│  ├─ index.json             ← 갤러리에 띄울 템플릿 목록
│  └─ dol-quiz\
│     ├─ renderer.html       ← ★ 결과물을 그리는 코드. 내용은 하나도 안 들어 있습니다
│     ├─ schema.json         ← ★ 이 파일이 편집기 폼을 만듭니다
│     ├─ sample.json         ← 데모·초기값 (원본 quiz의 채원이 문제 10개)
│     ├─ prompts.json        ← 문제 제안 36문항 (없어도 동작합니다)
│     ├─ meta.json           ← 이름·가격·폰트·음원 목록
│     └─ assets\
│        ├─ fonts\           ← woff2 3종
│        └─ audio\           ← 배경음악 (meta.json의 audio.files에 적힌 것만 심깁니다)
└─ app\
   ├─ index.html             ← 갤러리
   ├─ editor.html            ← 편집기
   ├─ assets\fonts\          ← Pretendard Variable (편집기 UI용, 자체 호스팅)
   ├─ css\app.css
   └─ js\
      ├─ store.js            ← 경로("quiz.0.options.2")로 읽고 쓰기 + 자동 저장
      ├─ imagestore.js       ← 사진만 따로 IndexedDB에. 작업 JSON에는 id만 (아래 §3-⑤)
      ├─ schema-form.js      ← ★ 스키마 → 폼. 템플릿이 늘어도 여기는 안 고칩니다
      ├─ build.js            ← 렌더러 + 데이터 + 폰트 → HTML 파일 하나
      └─ editor.js           ← 세 조각을 잇는 곳
```

---

## 3. 핵심 규칙 — 이걸 어기면 템플릿 3개째에 무너집니다

**① 렌더러는 자기 내용을 하드코딩하지 않습니다.**
`renderer.html`에는 "채원"이라는 글자가 한 번도 안 나옵니다. `const DATA = /*__DATA__*/ null;` 자리에
데이터가 주입되고 `buildSlides(DATA)`가 슬라이드를 만듭니다.

**② 폼은 만들지 않고 생성합니다.**
새 템플릿을 추가할 때 폼 코드를 짜지 않습니다. `schema.json`만 씁니다.
`schema-form.js`가 못 그리는 입력이 생기면, 폼을 새로 짜지 말고 **필드 타입을 하나 추가**하세요.
현재 지원: `text` `longtext` `select` `toggle` `answer` `image` `options` `repeater`(단순/카드) `worlds`

**③ 사용자 글은 전부 escape 합니다.**
남의 글을 그대로 화면에 심는 서비스입니다. `renderer.html`의 `esc()`/`inline()`을
거치지 않고 데이터를 출력하는 코드를 절대 넣지 마세요.

**④ 글자 크기는 사용자에게 묻지 않습니다.**
원본은 `qSize`/`optSize`를 손으로 맞췄습니다. 지금은 `autoQSize()`/`autoOptSize()`가
길이로 계산합니다. 원본 10문제와 대조해 곡선을 맞춰 뒀습니다.

**⑤ 사진은 작업 JSON에 넣지 않습니다.**
`store.js`는 작업 전체를 localStorage에 넣는데 한도가 origin당 5MB입니다. base64는
원본의 1.33배라 폰 사진 한 장이면 넘칩니다. 게다가 `store.js`의 저장 실패는
`console.warn`으로 삼켜져서, **화면에는 "저장됨"이 뜬 채 새로고침하면 작업이 날아갑니다.**
그래서 사진은 `imagestore.js`가 IndexedDB에 따로 들고, 작업 데이터에는 `img:xxxxxxxxxxxx`
문자열만 남깁니다. 미리보기와 내려받기 직전에 `hydrate()`가 그 id를 data: URI로 바꿔칩니다.
새 사진 칸을 스키마에 추가할 때 **`editor.js`는 고칠 필요가 없습니다** — 경로를 적어두는
대신 데이터 트리에서 `img:` 문자열을 찾습니다.

---

## 3-2. 편집기 화면 설계

**왼쪽은 편집 단계가 아니라 결과물의 목차입니다.** `STEP 1/2/3/4` 마법사를 쓰지 않습니다.
스키마의 `sections` 하나가 결과물의 장면 하나(`slide`)와 짝을 이루고, 문제는 데이터에서
펼쳐집니다. 그래서 왼쪽 목차 = 만들어지는 결과물입니다. 클릭하면 폼과 미리보기가 함께 갑니다.

- 폼은 한 화면에 쭉 이어집니다. 대신 **문제 카드는 접혀 있습니다.**
  접힌 줄에 문제·정답·월드가 보여서 10문제가 10줄로 정리됩니다.
- **정답은 `?`블록을 눌러 고릅니다.** 보기 중에서만 고를 수 있어 정답이 보기와
  어긋날 수 없습니다. 결과물에서 정답이 나오는 바로 그 물건입니다.
- 화면의 색은 조용하게, 채도는 결과물에만 둡니다. 금색은 정답 블록 한 군데에서만 씁니다.
- 오른쪽 무대 뒤 빛은 지금 보고 있는 장면의 세계(지상·지하·성)를 따라 바뀝니다.
- 밝은 화면 / 어두운 화면 모두 지원합니다. 시스템 설정을 따르고 직접 바꿀 수도 있습니다.

### 배경음악

**두 갈래입니다. `meta.json`의 `audio.files`가 갈림길입니다.**

| `audio.files` | 무슨 일이 일어나나 | 결과물 크기 |
|---|---|---|
| 파일 이름이 있으면 | 그 음원이 결과물에 심깁니다. **이름 순서대로 이어서 반복 재생** | **5.76MB** |
| 비어 있으면 | 렌더러가 **코드로 만든 8비트 배경음악**으로 자동 전환 | **1.76MB** |

지금은 음원 2개가 들어가 있어 `index.html`(5.75MB)과 사실상 같은 크기입니다.
음원을 바꾸려면 `assets/audio/`의 파일을 갈아 끼우고 `meta.json`의 `audio.files`를
고치면 됩니다. 개수 제한은 없습니다.

- 효과음은 **항상** WebAudio로 생성합니다. 음원 파일과 무관합니다.
- 합성 배경음악(`bgmTick`)은 파일이 없을 때만 돕니다. 128BPM, 8마디 한 바퀴(약 15초),
  베이스·아르페지오·멜로디 세 성부. **파일 크기가 1바이트도 늘지 않습니다.**
- 정답 팡파레가 울리면 배경음악이 잠깐 낮아졌다 돌아옵니다(`duck`). 두 방식 모두 동작합니다.
- **편집기 미리보기(iframe)에서는 음악이 나오지 않습니다.** 문구 하나 고칠 때마다
  음악이 울리면 편집을 못 합니다. `새 탭에서 열기`와 내려받은 파일에서는 정상입니다.
- 브라우저 정책상 **첫 조작 이후에** 소리가 납니다. 표지에서 조용한 건 정상입니다.

### 글씨체

**폰트 자리는 한 줄이 아니라 구간입니다.** `renderer.html`의
`/*__EMBED_FONTS__ … /*__/EMBED_FONTS__*/` 사이에는 **개발 서버 미리보기용**
`@font-face`가 파일 경로로 들어 있고, 빌드할 때 `build.js`가 그 구간을 base64로
심은 폰트로 통째로 갈아 끼웁니다. 두 벌이 같이 남으면 결과물이 있지도 않은
파일을 부르게 되므로 반드시 **치환이 아니라 구간 교체**여야 합니다.

> 2026-08-11 이전에는 이 구간이 그냥 빈 마커였습니다. 그래서 **편집기 미리보기가
> 폰트 없이 serif로 그려졌고**, 화면에서 보던 글씨와 내려받은 파일의 글씨가
> 달랐습니다. 폭을 재보면 `'PressStart2P', serif`가 미리보기에서 710px,
> 결과물에서 1080px이었습니다. 지금은 셋 다 픽셀 단위로 일치합니다.

편집기 UI(왼쪽 목차·폼)는 **Pretendard Variable**을 씁니다. 결과물의
`PretendardXB`는 ExtraBold 한 굵기뿐이라 UI 전체가 두꺼워집니다. 다만 픽셀
글씨(`.px` — 목차 번호·배지)는 결과물과 **같은 `Galmuri11.woff2` 파일**을
직접 불러 씁니다.

### 사진

**사진을 붙일 수 있는 자리는 네 군데입니다.** 전부 선택 사항이고, 비우면 예전과
똑같은 화면이 나옵니다.

| 자리 | 데이터 | 결과물에서 |
|---|---|---|
| 표지 | `cover.image` | 큰 글씨 위에 액자로 |
| 문제 | `quiz[].image` | 문제 글 아래에 |
| 보기 | `quiz[].options[].img` | 보기 카드 안에. 글자를 같이 쓰면 사진 아래에 붙습니다 |
| 마무리 | `ending.image` | 감사 인사 아래에 |

보기에 사진을 쓰려면 그 문제의 **보기 방식을 `사진`으로** 바꿉니다(`quiz[].layout`).
`글자`는 예전 그대로고, `번호만 (실물)`도 그대로입니다.

- **붙이는 순간 장변 1000px로 줄이고 WebP로 다시 굽습니다.** 원본을 그대로 두면
  결과물이 수십 MB가 되어 행사장 노트북에서 안 열립니다. 사진 한 장이 60~120KB쯤 됩니다.
- 사진 한 변은 보기 개수에 맞춰 계산합니다(5개 296px, 2개 340px). **한 줄을 넘기면
  세로가 모자라 글자가 상태바 위로 올라갑니다.** `renderer.html`의 `ph` 계산을 건드릴 때
  카드 좌우 여백 28px을 빼는 걸 잊지 마세요 — 여기서 한 번 깨졌습니다.
- 아이폰 사진(HEIC)은 브라우저가 못 엽니다. 그 자리에 JPG로 바꿔 달라고 적어줍니다.
- 보기는 예전에 글자 배열(`["아빠","엄마"]`)이었고 지금은 `{t,img}`입니다.
  **먼저 만든 작업과 `sample.json`은 아직 글자 배열입니다.** 편집기와 렌더러 양쪽의
  `optIn()`이 읽을 때 맞춰주니 둘 다 그대로 열립니다.

> 사진은 **이 브라우저에만** 있습니다(IndexedDB, §3-⑤). 다른 컴퓨터에서 이어서
> 작업하면 사진이 없습니다. 서버로 옮기는 건 M2입니다.

### 문제 제안 (`prompts.json`)

**`＋ 문제 추가`를 누르면 빈 카드가 아니라 제안 목록이 먼저 열립니다.**
편집기에서 사람이 가장 많이 막히는 지점은 UI가 아니라 "뭘 물어보지?"입니다.
샘플이 채워져 있어도 결국 자기 아이 이야기로 바꿔야 하고, 대개 네 번째 문제에서 멈춥니다.

- 6개 묶음 36문항. 고르면 문제와 **보기 5개 틀까지** 채워집니다. 사용자는 숫자만 바꿉니다.
- 이미 넣은 문제는 `srcId`로 알아보고 흐리게 표시합니다.
- 이름은 받침에 따라 자동으로 갈립니다 — `{아이}` → 채원이 / 지호,
  `{아이야}` → 채원아 / 지호야. 이걸 안 하면 "지호이가"처럼 어색해집니다.
- 템플릿에 `prompts.json`이 없으면 그냥 빈 카드가 추가됩니다. 필수가 아닙니다.

> **`scroll-behavior: smooth`는 이 레이아웃에서 동작하지 않습니다.** `editor.js`의
> `glide()`가 직접 굴립니다. 화면에 안 보이는 탭에서는 `requestAnimationFrame`이 멈추므로
> `document.hidden`일 때는 애니메이션 없이 바로 옮깁니다(자동화 검증 중 확인).

---

## 4. 편집기 ↔ 미리보기 통신

미리보기는 iframe 안에서 **진짜 렌더러가 돌아갑니다.** 리로드하지 않고 데이터만 갈아끼웁니다.
(리로드하면 보고 있던 장이 1장으로 되돌아가서 편집이 불가능합니다.)

| 방향 | 메시지 | 뜻 |
|---|---|---|
| 편집기 → 렌더러 | `studio:data` | 데이터 교체. 보던 장은 그대로 |
| 편집기 → 렌더러 | `studio:goto` | n번째 장으로 |
| 편집기 → 렌더러 | `studio:question` | Q3을 고치면 미리보기도 Q3으로 |
| 렌더러 → 편집기 | `studio:state` | 현재 위치·전체 장수·목차 |

렌더러가 노출하는 함수: `window.applyData(data)` `window.gotoSlide(i)` `window.gotoQuestion(no, kind)`

---

## 5. 검증 기록

- **원본 26장 재현** — 채원이 데이터를 새 형식으로 넣어 `buildSlides()` 결과를 원본
  `SLIDES` 배열과 대조. 24/26장 완전 일치, 차이 2건은 글자 크기이고 둘 다 원본보다
  **작아서** 넘칠 위험 없음.
- **빌드 산출물** — 샘플 데이터 + 폰트 3종 임베드 → **1.76MB** 단일 HTML, 마커 잔여 없음, 구문 정상.
  (원본 `index.html`은 음악 3MB가 더해져 6MB였습니다.)
- **escape** — 질문에 `<img src=x onerror=...>`를 넣어도 문자로 출력됨을 확인.
- **문제 수 가변** — 13문제 → 월드가 지상/지하/지상으로 이어짐. 1문제·0문제도 깨지지 않음.
- **월드 구성** — 월드마다 담을 문제 수를 따로 정합니다(`worlds: [3,7]`). 저장하는 값은
  실제로 담긴 수라 합이 늘 문제 수와 같습니다. 그래서 **문제를 더하거나 지울 때 그 문제가
  속한 월드를 함께 고쳐야 합니다**(`worldsAfterAdd`/`worldsAfterRemove`). 이걸 빠뜨리면
  WORLD 1에서 지웠는데 뒤 문제가 당겨 올라와 WORLD 2가 줄어듭니다. 숫자칸은 경계를 끄는
  것과 같습니다 — 줄이면 넘친 문제가 뒤 월드로 가고(뒤가 없으면 새 월드), 늘리면 뒤에서
  끌어옵니다. `worlds`가 없는 옛 작업은 `[5,5,…]`로 읽혀 전과 같은 자리에서 끊깁니다.
  편집기(`worldTag`)와 결과물(`worldPos`)이 같은 규칙이어야 합니다.
- **월드 구성 칸을 못 찾는 문제** — 처음 쓰는 사람은 문제 구획 맨 위의 칸을 그냥 지나칩니다.
  목차의 `WORLD n` 머리를 누를 수 있게 만들고(누르면 그 칸으로 굴러가 한 번 반짝입니다),
  칸에 테두리를 둘러 폼의 다른 줄과 구분했습니다.
- **보기 줄 나눔** — 글자 보기는 한 줄에 셋까지입니다. 폭에만 맡기면 글자 길이에 따라
  같은 게임 안에서 3+2였다 4+1이었다 합니다. 셋째 뒤에 높이 0짜리 `.opt-br`을 끼워
  줄을 끊되, **마지막 보기 뒤에는 넣지 않습니다** — 빈 줄의 row-gap 22px만큼 아래가
  벌어집니다. 보기 2·3·4·5개로 재서 2 / 3 / 3+1 / 3+2 확인했고 3개일 때 높이가
  2개일 때와 같습니다(43px). 사진 보기는 한 줄에 나란히 놓고 `--ph`를 개수로
  계산하는 배치라 그대로 둡니다.

- **배경음악** — 브라우저 빌드 결과물에 음원 2곡이 심겼고(`base64` 5개 = 폰트 3 + 음원 2),
  `BGM_SRC`에 정상 주입, 마커 잔여 없음, **5.76MB** (원본 `index.html` 5.75MB와 사실상 동일).
  합성 배경음악 코드는 파일이 없을 때를 위해 남아 있습니다.
- **브라우저 확인** — 편집기를 열어 목차 클릭 → 폼 이동 → 카드 펼침 → 미리보기 이동까지
  동작 확인. 브라우저에서 조립한 결과물도 1.8MB·폰트 3종 임베드·마커 잔여 없음.
  밝은 화면/어두운 화면 모두 확인.

> **2026-08-11부터 채원이 데이터가 `sample.json`에 들어 있습니다.** 원본 `quiz/index.html`의
> 문제 10개를 그대로 옮겼습니다(이전에는 가상의 이름을 썼고, Q1 보기도 임의값이었습니다).
> 이 저장소는 **public**입니다 — 이름·출생 몸무게·첫 말 날짜가 공개된다는 뜻입니다.

---

## 6. TODO

### 🔴 지금 할 것 (M1 마무리)

- [ ] **제3자 데이터로 처음부터 만들어보기** — 남의 돌잔치 정보로 10문제를 채웠을 때
      결과물이 실제로 예쁜지. **M1의 진짜 합격 기준입니다.**
- [ ] 좁은 화면(태블릿·폰)에서 편집 가능한지 확인 — 코드는 넣었지만 실제로 못 봤습니다
- [ ] 되돌리기(실행 취소) — 지금은 자동 저장만 있고 되돌릴 방법이 없습니다

### 🟡 M2로 넘길 것

- [ ] 서버 빌드로 이관 — 렌더러 소스 보호, 폰트 서브셋(1.7MB → 100KB 이하), 공유 링크
- [ ] 사진을 서버로 이관 — 지금은 IndexedDB라 **브라우저를 옮기면 사진이 따라오지 않습니다**
- [ ] 로그인·프로젝트 저장 (지금은 localStorage 하나뿐)
- [ ] 2번째 템플릿(돌잔치 초대장) 투입 — 스키마–폼 파이프라인의 구멍은 여기서 드러납니다

### ⚠️ 상용화 전 반드시

- [ ] **폰트 라이선스 원문 확인** — 파일에 임베드해 파는 건 재배포입니다.
      3종 모두 OFL로 보이지만 확인 전까지는 확정이 아닙니다. 결과물에 고지도 넣어야 합니다.
- [ ] **배경음악 음원의 라이선스 근거를 문서로 남기기** — 현재 `assets/audio/`의 두 곡은
      **사용자가 저작권 문제 없음을 확인해 넣은 것입니다.** 판매를 시작하면 결과물마다
      이 음원이 배포되므로, 출처·이용범위를 한 줄이라도 적어두면 나중에 분쟁 시 근거가 됩니다.
      음원을 빼고 싶으면 `meta.json`의 `audio.files`를 `[]`로 두면 됩니다
      (합성 배경음악으로 자동 전환, 결과물 3MB 감소).
- [ ] 개인정보 처리방침·보관기간, 청약철회 약관

---

## 7. 배포

**서버가 필요 없습니다.** 빌드(폰트 심고 데이터 꽂기)를 브라우저에서 하고 저장은
localStorage라, 지금은 정적 파일만 올리면 됩니다. **Supabase는 아직 쓸 데가 없습니다.**
(사용자 결과물의 공유 링크를 만들 때 — M2 — 그때 저장소와 DB가 필요해집니다.)

Vercel을 쓰는 실질적 이유는 하나입니다. **비공개 저장소도 무료로 붙습니다.**
(GitHub Pages는 무료 플랜에서 public 저장소만 됩니다.)

### 처음 한 번

1. [vercel.com](https://vercel.com) 에 **GitHub 계정으로** 로그인
2. `Add New` → `Project` → `you4ranghe/studio` 선택
3. 설정 — **아무것도 안 건드리고 아래처럼 비웁니다**

   | 항목 | 값 |
   |---|---|
   | Framework Preset | **Other** |
   | Build Command | 비움 |
   | Output Directory | 비움 (저장소 루트) |
   | Install Command | 비움 |

4. `Deploy` → 1~2분 뒤 `https://studio-….vercel.app`

이후 `main`에 푸시하면 자동으로 다시 배포됩니다.

### 🔴 배포 직후 반드시 확인할 것

- [ ] `/` 를 열면 갤러리가 뜨는가
- [ ] **`/_plan/기획서.md` 가 404인가** ← 200이면 가격·수익 구조가 공개된 것입니다. 즉시 내리세요
- [ ] `/WBS.md` 가 404인가

`.vercelignore`가 `_plan`·`*.md`·`dev.js`를 제외합니다. 배포에 올라가는 건 `app/`과
`templates/` 뿐입니다.

### 설정 파일

- `vercel.json` — 루트 `/` 를 `/app/` 으로 **redirect**. rewrite면 안 됩니다.
  주소가 `/` 로 남으면 `./js/build.js` 같은 상대 경로가 `/js/build.js` 로 잘못 풀립니다.
  (`dev.js`도 같은 이유로 302를 보냅니다. 두 환경을 똑같이 맞춰 뒀습니다.)
- 폰트(woff2)만 1년 캐시합니다. 2MB이고 바뀌지 않습니다.

> ⚠️ **`vercel.json`에는 주석용 키를 넣지 마세요.** Vercel이 스키마를 엄격하게 검사해서
> 모르는 키가 있으면 배포가 통째로 막힙니다
> (`Invalid request: should NOT have additional property '_'`).
> 설명은 이 문서에 씁니다. 반대로 `templates/*.json`은 우리 코드만 읽으므로 `_` 키를 써도 됩니다.

### ⚠️ 감수하는 것 — 돈 받기 전에 반드시 바꿔야 합니다

정적 배포는 **`templates/dol-quiz/renderer.html` 소스를 그대로 공개합니다.**
무료 시제품 단계에서는 괜찮지만, 유료로 팔기 시작하면 무료 사용자가 템플릿을 통째로
가져갈 수 있습니다. 기획서 §7에서 서버 빌드를 택한 이유가 이것입니다. **M2 필수 과제.**

---

## 8. 기술 메모

- `renderer.html`은 원본 `slides.src.html`에서 1회성 스크립트로 변환해 만들었습니다.
  **지금부터는 `renderer.html`이 원본입니다.** 원본 퀴즈 저장소와는 이제 별개로 갑니다.
- 무대는 1920×1080 고정 후 `transform:scale()`로 맞춥니다. 그래서 iframe이 작아도
  결과물과 똑같이 보입니다.
- 효과음은 파일 없이 WebAudio로 생성합니다. 브라우저 정책상 첫 조작 이후에 납니다.
- 폰트는 `build.js`에서 한 번만 받아 캐시합니다(1.7MB). 내려받기를 여러 번 눌러도 다시 안 받습니다.
