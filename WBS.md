# 스튜디오 — 작업 문서

> **새 세션은 이 파일을 먼저 읽으세요.** 제품 설계와 근거는 [`_plan/기획서.md`](_plan/기획서.md)에 있습니다.

- 최종 업데이트: 2026-08-10
- 무엇: 폼을 채우면 돌잔치·청첩장·행사 퀴즈 페이지를 **HTML 파일 하나로** 뽑아주는 유료 서비스
- 현재 단계: **M1 (파이프라인 증명)** — 서버·로그인·결제 없음
- 출발점: `C:\Users\ibank\quiz` 의 돌잔치 퀴즈. 그 `slides.src.html`이 1번 템플릿이 됐습니다.

---

## 1. 실행

```powershell
cd C:\Users\ibank\studio
npm run dev
```

| 주소 | 화면 |
|---|---|
| http://localhost:5173/app/index.html | 갤러리 |
| http://localhost:5173/app/editor.html?t=dol-quiz | 편집기 |

> `file://`로 열면 안 됩니다. 브라우저가 `fetch()`로 템플릿 파일을 읽어야 해서
> 반드시 개발 서버를 거쳐야 합니다. 의존성은 없고 `dev.js`는 표준 라이브러리만 씁니다.

---

## 2. 구조

```
studio\
├─ WBS.md                    ← 이 문서
├─ _plan\기획서.md            ← 제품 기획 (설계 근거·마일스톤·리스크)
├─ dev.js                    ← 개발 서버 (의존성 없음)
├─ templates\
│  ├─ index.json             ← 갤러리에 띄울 템플릿 목록
│  └─ dol-quiz\
│     ├─ renderer.html       ← ★ 결과물을 그리는 코드. 내용은 하나도 안 들어 있습니다
│     ├─ schema.json         ← ★ 이 파일이 편집기 폼을 만듭니다
│     ├─ sample.json         ← 데모·초기값 (가상의 아기 '하윤')
│     ├─ prompts.json        ← 문제 제안 36문항 (없어도 동작합니다)
│     ├─ meta.json           ← 이름·가격·폰트 목록
│     └─ assets\fonts\       ← woff2 3종
└─ app\
   ├─ index.html             ← 갤러리
   ├─ editor.html            ← 편집기
   ├─ assets\fonts\          ← Pretendard Variable (편집기 UI용, 자체 호스팅)
   ├─ css\app.css
   └─ js\
      ├─ store.js            ← 경로("quiz.0.options.2")로 읽고 쓰기 + 자동 저장
      ├─ schema-form.js      ← ★ 스키마 → 폼. 템플릿이 늘어도 여기는 안 고칩니다
      ├─ build.js            ← 렌더러 + 데이터 + 폰트 → HTML 파일 하나
      └─ editor.js           ← 세 조각을 잇는 곳
```

---

## 3. 핵심 규칙 — 이걸 어기면 템플릿 3개째에 무너집니다

**① 렌더러는 자기 내용을 하드코딩하지 않습니다.**
`renderer.html`에는 "채원"도 "하윤"도 없습니다. `const DATA = /*__DATA__*/ null;` 자리에
데이터가 주입되고 `buildSlides(DATA)`가 슬라이드를 만듭니다.

**② 폼은 만들지 않고 생성합니다.**
새 템플릿을 추가할 때 폼 코드를 짜지 않습니다. `schema.json`만 씁니다.
`schema-form.js`가 못 그리는 입력이 생기면, 폼을 새로 짜지 말고 **필드 타입을 하나 추가**하세요.
현재 지원: `text` `longtext` `select` `toggle` `answer` `repeater`(단순/카드)

**③ 사용자 글은 전부 escape 합니다.**
남의 글을 그대로 화면에 심는 서비스입니다. `renderer.html`의 `esc()`/`inline()`을
거치지 않고 데이터를 출력하는 코드를 절대 넣지 마세요.

**④ 글자 크기는 사용자에게 묻지 않습니다.**
원본은 `qSize`/`optSize`를 손으로 맞췄습니다. 지금은 `autoQSize()`/`autoOptSize()`가
길이로 계산합니다. 원본 10문제와 대조해 곡선을 맞춰 뒀습니다.

---

## 3-2. 편집기 화면 설계

**왼쪽은 편집 단계가 아니라 결과물의 목차입니다.** `STEP 1/2/3/4` 마법사를 쓰지 않습니다.
스키마의 `sections` 하나가 결과물의 장면 하나(`slide`)와 짝을 이루고, 문제는 데이터에서
펼쳐집니다. 그래서 왼쪽 목차 = 만들어지는 결과물입니다. 클릭하면 폼과 미리보기가 함께 갑니다.

- 폼은 한 화면에 쭉 이어집니다. 대신 **문제 카드는 접혀 있습니다.**
  접힌 줄에 문제·정답·월드가 보여서 10문제가 10줄로 정리됩니다.
- **정답은 `?`블록을 눌러 고릅니다.** 보기 중에서만 고를 수 있어 정답이 보기와
  어긋날 수 없습니다. 결과물에서 정답이 나오는 바로 그 물건입니다.
- 화면의 색은 조용하게, 채도는 결과물에만 둡니다. 금색은 정답 블록 한 군데에서만 씁니다.
- 오른쪽 무대 뒤 빛은 지금 보고 있는 장면의 세계(지상·지하·성)를 따라 바뀝니다.
- 밝은 화면 / 어두운 화면 모두 지원합니다. 시스템 설정을 따르고 직접 바꿀 수도 있습니다.

### 문제 제안 (`prompts.json`)

**`＋ 문제 추가`를 누르면 빈 카드가 아니라 제안 목록이 먼저 열립니다.**
편집기에서 사람이 가장 많이 막히는 지점은 UI가 아니라 "뭘 물어보지?"입니다.
샘플이 채워져 있어도 결국 자기 아이 이야기로 바꿔야 하고, 대개 네 번째 문제에서 멈춥니다.

- 6개 묶음 36문항. 고르면 문제와 **보기 5개 틀까지** 채워집니다. 사용자는 숫자만 바꿉니다.
- 이미 넣은 문제는 `srcId`로 알아보고 흐리게 표시합니다.
- 이름은 받침에 따라 자동으로 갈립니다 — `{아이}` → 하윤이 / 지호,
  `{아이야}` → 하윤아 / 지호야. 이걸 안 하면 "지호이가"처럼 어색해집니다.
- 템플릿에 `prompts.json`이 없으면 그냥 빈 카드가 추가됩니다. 필수가 아닙니다.

> **`scroll-behavior: smooth`는 이 레이아웃에서 동작하지 않습니다.** `editor.js`의
> `glide()`가 직접 굴립니다. 화면에 안 보이는 탭에서는 `requestAnimationFrame`이 멈추므로
> `document.hidden`일 때는 애니메이션 없이 바로 옮깁니다(자동화 검증 중 확인).

---

## 4. 편집기 ↔ 미리보기 통신

미리보기는 iframe 안에서 **진짜 렌더러가 돌아갑니다.** 리로드하지 않고 데이터만 갈아끼웁니다.
(리로드하면 보고 있던 장이 1장으로 되돌아가서 편집이 불가능합니다.)

| 방향 | 메시지 | 뜻 |
|---|---|---|
| 편집기 → 렌더러 | `studio:data` | 데이터 교체. 보던 장은 그대로 |
| 편집기 → 렌더러 | `studio:goto` | n번째 장으로 |
| 편집기 → 렌더러 | `studio:question` | Q3을 고치면 미리보기도 Q3으로 |
| 렌더러 → 편집기 | `studio:state` | 현재 위치·전체 장수·목차 |

렌더러가 노출하는 함수: `window.applyData(data)` `window.gotoSlide(i)` `window.gotoQuestion(no, kind)`

---

## 5. 검증 기록

- **원본 26장 재현** — 채원이 데이터를 새 형식으로 넣어 `buildSlides()` 결과를 원본
  `SLIDES` 배열과 대조. 24/26장 완전 일치, 차이 2건은 글자 크기이고 둘 다 원본보다
  **작아서** 넘칠 위험 없음.
- **빌드 산출물** — 샘플 데이터 + 폰트 3종 임베드 → **1.76MB** 단일 HTML, 마커 잔여 없음, 구문 정상.
  (원본 `index.html`은 음악 3MB가 더해져 6MB였습니다.)
- **escape** — 질문에 `<img src=x onerror=...>`를 넣어도 문자로 출력됨을 확인.
- **문제 수 가변** — 13문제 → 월드가 지상/지하/지상으로 이어짐. 1문제·0문제도 깨지지 않음.

- **브라우저 확인** — 편집기를 열어 목차 클릭 → 폼 이동 → 카드 펼침 → 미리보기 이동까지
  동작 확인. 브라우저에서 조립한 결과물도 1.8MB·폰트 3종 임베드·마커 잔여 없음.
  밝은 화면/어두운 화면 모두 확인.

> 검증 스크립트는 임시 폴더에서만 돌렸습니다. 채원이 데이터는 **이 저장소에 없습니다.**

---

## 6. TODO

### 🔴 지금 할 것 (M1 마무리)

- [ ] **제3자 데이터로 처음부터 만들어보기** — 남의 돌잔치 정보로 10문제를 채웠을 때
      결과물이 실제로 예쁜지. **M1의 진짜 합격 기준입니다.**
- [ ] 좁은 화면(태블릿·폰)에서 편집 가능한지 확인 — 코드는 넣었지만 실제로 못 봤습니다
- [ ] 되돌리기(실행 취소) — 지금은 자동 저장만 있고 되돌릴 방법이 없습니다

### 🟡 M2로 넘길 것

- [ ] 서버 빌드로 이관 — 렌더러 소스 보호, 폰트 서브셋(1.7MB → 100KB 이하), 공유 링크
- [ ] 사진 업로드 (현재 템플릿은 픽셀 아트만 씁니다)
- [ ] 로그인·프로젝트 저장 (지금은 localStorage 하나뿐)
- [ ] 2번째 템플릿(돌잔치 초대장) 투입 — 스키마–폼 파이프라인의 구멍은 여기서 드러납니다

### ⚠️ 상용화 전 반드시

- [ ] **폰트 라이선스 원문 확인** — 파일에 임베드해 파는 건 재배포입니다.
      3종 모두 OFL로 보이지만 확인 전까지는 확정이 아닙니다. 결과물에 고지도 넣어야 합니다.
- [ ] **배경음악** — 원본 저장소의 음원은 저작권이 정리되지 않았습니다.
      이 저장소에는 **가져오지 않았습니다.** 켤 때는 로열티프리 음원부터 확보하세요.
- [ ] 개인정보 처리방침·보관기간, 청약철회 약관

---

## 7. 배포

**서버가 필요 없습니다.** 빌드(폰트 심고 데이터 꽂기)를 브라우저에서 하고 저장은
localStorage라, 지금은 정적 파일만 올리면 됩니다. **Supabase는 아직 쓸 데가 없습니다.**
(사용자 결과물의 공유 링크를 만들 때 — M2 — 그때 저장소와 DB가 필요해집니다.)

Vercel을 쓰는 실질적 이유는 하나입니다. **비공개 저장소도 무료로 붙습니다.**
(GitHub Pages는 무료 플랜에서 public 저장소만 됩니다.)

### 처음 한 번

1. [vercel.com](https://vercel.com) 에 **GitHub 계정으로** 로그인
2. `Add New` → `Project` → `you4ranghe/studio` 선택
3. 설정 — **아무것도 안 건드리고 아래처럼 비웁니다**

   | 항목 | 값 |
   |---|---|
   | Framework Preset | **Other** |
   | Build Command | 비움 |
   | Output Directory | 비움 (저장소 루트) |
   | Install Command | 비움 |

4. `Deploy` → 1~2분 뒤 `https://studio-….vercel.app`

이후 `main`에 푸시하면 자동으로 다시 배포됩니다.

### 🔴 배포 직후 반드시 확인할 것

- [ ] `/` 를 열면 갤러리가 뜨는가
- [ ] **`/_plan/기획서.md` 가 404인가** ← 200이면 가격·수익 구조가 공개된 것입니다. 즉시 내리세요
- [ ] `/WBS.md` 가 404인가

`.vercelignore`가 `_plan`·`*.md`·`dev.js`를 제외합니다. 배포에 올라가는 건 `app/`과
`templates/` 뿐입니다.

### 설정 파일

- `vercel.json` — 루트 `/` 를 `/app/` 으로 **redirect**. rewrite면 안 됩니다.
  주소가 `/` 로 남으면 `./js/build.js` 같은 상대 경로가 `/js/build.js` 로 잘못 풀립니다.
  (`dev.js`도 같은 이유로 302를 보냅니다. 두 환경을 똑같이 맞춰 뒀습니다.)
- 폰트(woff2)만 1년 캐시합니다. 2MB이고 바뀌지 않습니다.

### ⚠️ 감수하는 것 — 돈 받기 전에 반드시 바꿔야 합니다

정적 배포는 **`templates/dol-quiz/renderer.html` 소스를 그대로 공개합니다.**
무료 시제품 단계에서는 괜찮지만, 유료로 팔기 시작하면 무료 사용자가 템플릿을 통째로
가져갈 수 있습니다. 기획서 §7에서 서버 빌드를 택한 이유가 이것입니다. **M2 필수 과제.**

---

## 8. 기술 메모

- `renderer.html`은 원본 `slides.src.html`에서 1회성 스크립트로 변환해 만들었습니다.
  **지금부터는 `renderer.html`이 원본입니다.** 원본 퀴즈 저장소와는 이제 별개로 갑니다.
- 무대는 1920×1080 고정 후 `transform:scale()`로 맞춥니다. 그래서 iframe이 작아도
  결과물과 똑같이 보입니다.
- 효과음은 파일 없이 WebAudio로 생성합니다. 브라우저 정책상 첫 조작 이후에 납니다.
- 폰트는 `build.js`에서 한 번만 받아 캐시합니다(1.7MB). 내려받기를 여러 번 눌러도 다시 안 받습니다.
